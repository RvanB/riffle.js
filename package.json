{
  "name": "riffle",
  "version": "0.2.0",
  "description": "Page-turning book viewer with show-through translucency.",
  "type": "module",
  "main": "dist/riffle.min.js",
  "module": "dist/riffle.min.js",
  "exports": {
    ".": {
      "import": "./dist/riffle.min.js",
      "default": "./src/index.js"
    }
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --minify --sourcemap --outfile=dist/riffle.min.js && esbuild src/loading/pdfWorker.js --bundle --format=esm --minify --sourcemap --outfile=dist/pdfWorker.js && esbuild src/loading/pdfiumWorker.js --bundle --format=esm --minify --sourcemap --outfile=dist/pdfiumWorker.js && esbuild src/loading/imageWorker.js --bundle --format=esm --minify --sourcemap --outfile=dist/imageWorker.js",
    "docs": "rm -rf docs && jsdoc -c jsdoc.json && ./scripts/prune-docs.sh"
  },
  "devDependencies": {
    "docdash": "^2.0.2",
    "esbuild": "^0.23.0",
    "jsdoc": "^4.0.5"
  }
}
